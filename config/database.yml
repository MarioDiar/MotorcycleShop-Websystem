# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
#default: &default
 # adapter: postgresql
  #database: bajajcolima
  #username: postgres
  #password: <%= ENV['APP_USER_POSTGRES_PASSWORD'] %>
  #pool: 5
  #timeout: 5000

default: &default
  adapter: postgresql
  enconding: unicode
  pool: 5

development:
  <<: *default
  database: mariodiar_development

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: mariodiar_test 

production:
  <<: *default
  database: mariodiar_production
  username: mariodiar_production
  password: <%= ENV['DATABASE_PASSWORD'] %>
